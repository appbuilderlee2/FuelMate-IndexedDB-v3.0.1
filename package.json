{
  "name": "fuelmate-indexeddb",
  "private": true,
  "version": "3.3.0",
  "type": "module",
  "scripts": {
    "prepare:static": "node scripts/copy-static-js.mjs",
    "build:css": "node scripts/generate-tailwind-safelist.mjs && tailwindcss -c tailwind.config.js -i styles/tailwind.css -o public/app.css --minify",
    "predev": "npm run prepare:static && npm run build:css",
    "prebuild": "npm run prepare:static && npm run build:css",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@tailwindcss/cli": "^4.1.11",
    "@types/node": "^22.14.0",
    "tailwindcss": "^4.1.11",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
